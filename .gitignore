# Python
__pycache__/
*.py[oc]
*.egg-info/
.venv/
.pytest_cache/
.coverage
htmlcov/

# Node
node_modules/
dist/

# Env
.env
.env.*
!.env.example

# Tracking-loop working data (backend/scripts/run_tracking_loop.py). Raw per-sample
# responses and the local usage ledger are large/regenerable and stay untracked; the
# admitted trend-series snapshots and run audit trail under backend/data/tracking/ are
# the one deliverable DESIGN_v1 §6.8 says can't be rebuilt later, so they ARE committed.
backend/data/responses/
backend/data/usage/

# OS
.DS_Store
